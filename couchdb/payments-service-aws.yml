server:
  port: 8081
eureka:
  instance:
    preferIpAddress: true
    ip-address: ${DOCKER_HOST_IP}
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI}
couchdb:
  username: wdm
  password: wdm-2019
  database: payments
  host: ${COUCH_URI}
  port: 5984
